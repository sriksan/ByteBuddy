//
//  PlanView.swift
//  ByteBuddy
//
//  Created by Justin Li on 4/3/24.
//

/*
 import SwiftUI
struct PlanView: View {
    var body: some View {
        VStack {
            Text("Diet Plan")
                .font(.title)
                .padding()
            
            TextField("Plan Name", text: .constant(""))
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            TextField("Daily Calorie Limit", text: .constant(""))
                .textFieldStyle(RoundedBorderTextFieldStyle())
                .padding()
            
            Button(action: {
                // Create plan action
            }) {
                Text("Create Plan")
            }
            
            List(0..<5) { index in
                Text("Meal \(index + 1)")
            }
            
            Spacer()
        }
    }
}
#Preview {
    PlanView()
 }
*/
